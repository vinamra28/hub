import React from 'react';
import { GridItem } from '@patternfly/react-core';

const Details: React.FC = () => {
  return (
    <React.Fragment>
      <GridItem span={12}>Add Resources detail here</GridItem>
    </React.Fragment>
  );
};
export default Details;
